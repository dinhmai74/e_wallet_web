import React from "react";
import { Screen } from "components";

interface Props {}

export const MovieTicketChoseInfoScreen: React.FC<Props> = props => {
  const {} = props;
  return <Screen>Movie MovieTicketChoseInfoScreen</Screen>;
};
