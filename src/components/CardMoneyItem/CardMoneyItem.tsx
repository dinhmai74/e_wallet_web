import { HeroCard, Screen } from "components";
import React from "react";
import { Hero } from "screens/Home/components/Hero";
import { images } from "theme";

export const CardMoneyItem: React.FC = () => {
  const renderRow = (data, i) => {};

  return <div />;
};
