import React from "react";
import { useCss } from "react-use";
import cx from "classnames";

interface Props {}
export const Footer: React.FC<Props> = props => {
  return <div className="footer bg__bg-5 mt-8" />;
};
